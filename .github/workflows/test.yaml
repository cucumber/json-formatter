name: test

on:
  push:
    branches:
      - main
      - fix/working-ci
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Make
        run: make